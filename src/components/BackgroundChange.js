import React from 'react';

function BgChange(params) {
    //toDo: make a function that will change backgrounds according to the game statistics - les time left, scarier the bg. Also could be changed according to locations
}